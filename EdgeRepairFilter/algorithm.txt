1.记录点的【】【】序号坐标
2.每次添加一组线段，都进行二分查找，从而确认最左和最右值。
	在每个线段方向固定的情况下，只有四种有问题的情况,均存在后加入组的右值比左值大
	([)]：留外边的
	([])
	[()]
	[(])

struct ALine{
			int contour;
			int BeginN;
			int BeginX;
			int BeginY;
			int EndN;
			int EndX;
			int EndY;
		};

3.所有的counterpixel，都代换成pixel。ds

4.添加绝对编号？Iterator